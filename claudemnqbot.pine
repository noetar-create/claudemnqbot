//@version=6
strategy("claudemnqbot v1", shorttitle="cmb", overlay=true, default_qty_value=5, default_qty_type=strategy.fixed)

nyHour   = hour(time,   "America/New_York")
nyMinute = minute(time, "America/New_York")
tod      = nyHour * 60 + nyMinute

in930  = tod >= 570 and tod < 580
in950  = tod >= 590 and tod < 610
in1020 = tod >= 620 and tod < 640
in1050 = tod >= 650 and tod < 670
in1120 = tod >= 680 and tod < 700
in1150 = tod >= 710 and tod < 730
in1350 = tod >= 830 and tod < 850

inAnyWindow = in930 or in950 or in1020 or in1050 or in1120 or in1150 or in1350

if inAnyWindow and strategy.position_size == 0
    strategy.entry("L", close >= open ? strategy.long : strategy.short)

if nyHour >= 16
    strategy.close_all()

bgcolor(inAnyWindow ? color.new(color.blue, 92) : na)
